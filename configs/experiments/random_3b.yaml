include:
  model: configs/model_50m.yaml
  train: configs/train_base.yaml

experiment:
  name: random_3b
  mode: normal
  init_checkpoint: null

train:
  max_tokens: 3000000000

runtime:
  output_dir: runs/random_3b
