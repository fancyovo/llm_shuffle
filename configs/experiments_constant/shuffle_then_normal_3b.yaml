include:
  model: configs/model_50m.yaml
  train: configs/train_constant_lr.yaml

experiment:
  name: shuffle_then_normal_3b_constant_lr
  mode: normal
  init_checkpoint: runs_constant/shuffle_pretrain_1b/checkpoints/latest.pt

train:
  max_tokens: 3000000000

runtime:
  output_dir: runs_constant/shuffle_then_normal_3b
